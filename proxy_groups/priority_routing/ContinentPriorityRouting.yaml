# Asia default router
- name: "AF-PRIORITY-ROUTING"
  type: fallback
  use:
    ++africa_default++
  url: 'http://www.gstatic.com/generate_204'
  interval: 300

# Antarctica default router
- name: "AN-PRIORITY-ROUTING"
  type: fallback
  use:
    ++antarctica_default++
  url: 'http://www.gstatic.com/generate_204'
  interval: 300

# Asia default router
- name: "AS-PRIORITY-ROUTING"
  type: fallback
  use:
    ++asia_default++
  url: 'http://www.gstatic.com/generate_204'
  interval: 300

# Europe default router
- name: "EU-PRIORITY-ROUTING"
  type: fallback
  use:
    ++europe_default++
  url: 'http://www.gstatic.com/generate_204'
  interval: 300

# North American default router
- name: "NA-PRIORITY-ROUTING"
  type: fallback
  use:
    ++north_american_default++
  url: 'http://www.gstatic.com/generate_204'
  interval: 300
  
# Oceania default router
- name: "OC-PRIORITY-ROUTING"
  type: fallback
  use:
    ++oceania_default++
  url: 'http://www.gstatic.com/generate_204'
  interval: 300

# South American default router
- name: "SA-PRIORITY-ROUTING"
  type: fallback
  use:
    ++south_american_default++
  url: 'http://www.gstatic.com/generate_204'
  interval: 300